# Phase 8.17 Status — Public Result Lookup Page Dry-Run Contract

Status: ready for local validation.

The phase adds a fake-executor dry-run for future `/r/[publicId]` database lookup behavior. It does not enable the actual public result page lookup.

Expected gate:

```bash
npm run dryrun:public-lookup-page
```

Expected evidence:

```text
docs/evidence/public-result-lookup-page-dry-run-contract-latest.json
```
